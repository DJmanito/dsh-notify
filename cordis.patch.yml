# @djmanito/dsh-notify bundle patch(dsh.bundle manifest 要求的加载配置)
# 单包单插件:装一次,设置页「通知设置」+ 浏览器系统通知全部到位。
# 惯例:id = 短名, name = 完整包名(含 scope)。
- insert:
    - id: dsh-notify
      name: '@djmanito/dsh-notify'
